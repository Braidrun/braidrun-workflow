---
name: test-workflow-long-running
version: 1.0.0
tags:
- test
- long-running
description: Long-running and progress-monitoring test - tests WebSocket live updates, progress tracking, and cancellation
translations:
  en:
    name: "Long Running Test"
    description: "Long-running and progress-monitoring test - tests WebSocket live updates, progress tracking, and cancellation"
  zh:
    name: "长时间运行测试"
    description: "长时间运行和进度监控测试——测试 WebSocket 实时更新、进度跟踪和取消。"
  zhHant:
    name: "長時間執行測試"
    description: "長時間執行和進度監控測試——測試 WebSocket 即時更新、進度追蹤和取消。"
  ja:
    name: "長時間実行テスト"
    description: "長時間実行と進捗監視のテスト - WebSocket のライブ更新、進捗追跡、キャンセルをテストします。"
  ko:
    name: "장시간 실행 테스트"
    description: "장시간 실행 및 진행 상황 모니터링 테스트 - WebSocket 실시간 업데이트, 진행 추적, 취소를 테스트합니다."
  es:
    name: "Prueba de larga ejecución"
    description: "Prueba de ejecución prolongada y seguimiento del progreso: prueba las actualizaciones en vivo de WebSocket, el seguimiento del progreso y la cancelación."
  fr:
    name: "Test de longue exécution"
    description: "Test d’exécution longue et de suivi de progression - teste les mises à jour en direct via WebSocket, le suivi de progression et l’annulation."
  de:
    name: "Laufzeit Test"
    description: "Test für Langläufer und Fortschrittsüberwachung - prüft Live-Updates über WebSocket, Fortschrittsverfolgung und Abbruch."
  ar:
    name: "اختبار التشغيل الطويل"
    description: "اختبار التشغيل الطويل ومراقبة التقدم - يختبر التحديثات المباشرة عبر WebSocket، وتتبع التقدم، والإلغاء."
  pt:
    name: "Teste de execução longa"
    description: "Teste de execução prolongada e monitoramento de progresso - testa atualizações ao vivo via WebSocket, rastreamento de progresso e cancelamento."
  vi:
    name: "Kiểm thử chạy dài"
    description: "Kiểm thử chạy dài và giám sát tiến độ - kiểm tra cập nhật trực tiếp qua WebSocket, theo dõi tiến độ và hủy."
variables:
  output_dir: "./output"
  total_items: '5'
  demo_mode: 'true'
agents:
  worker:
    preset: universal
    description: "Handles precise task execution responsibilities in this test workflow."
    translations:
      en:
        description: "Handles precise task execution responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的精确任务执行职责。"
      zhHant:
        description: "負責此測試工作流程中的精確任務執行職責。"
      ja:
        description: "このテストワークフローで正確なタスク実行を担当します。"
      ko:
        description: "이 테스트 워크플로에서 정확한 작업 실행 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de ejecución precisa de tareas en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de exécution précise des tâches dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für präzise Aufgabenausführung."
      ar:
        description: "يتولى مسؤوليات التنفيذ الدقيق للمهام في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de execução precisa de tarefas neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò thực thi tác vụ chính xác trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a precise assistant. Follow instructions exactly. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      - shell
      max_iterations: 100
      session_id_strategy: auto
workflow:
- step: prepare
  agent: worker
  priority: 10
  input: |-
    Create the directory {{var:output_dir}}/long_running if it doesn't exist.
    Create a file {{var:output_dir}}/long_running/config.json with this content:
    {"total_items": {{var:total_items}}, "status": "started"}
    Then call exit.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/prepare.md.
- step: process_batch_1
  agent: worker
  depends_on:
  - prepare
  input: |-
    Create a file {{var:output_dir}}/long_running/batch_1.txt with content:
    "Batch 1: Processing items 1-2 of {{var:total_items}}"
    Then call exit.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/process_batch_1.md.
- step: process_batch_2
  agent: worker
  depends_on:
  - process_batch_1
  input: |-
    Create a file {{var:output_dir}}/long_running/batch_2.txt with content:
    "Batch 2: Processing items 3-4 of {{var:total_items}}"
    Then call exit.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/process_batch_2.md.
- step: process_batch_3
  agent: worker
  depends_on:
  - process_batch_2
  input: |-
    Create a file {{var:output_dir}}/long_running/batch_3.txt with content:
    "Batch 3: Processing item 5 of {{var:total_items}}"
    Then call exit.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/process_batch_3.md.
- step: validate_results
  agent: worker
  depends_on:
  - process_batch_3
  input: |-
    Read all batch files in {{var:output_dir}}/long_running/ directory.
    Verify that batch_1.txt, batch_2.txt, and batch_3.txt all exist.
    Create {{var:output_dir}}/long_running/validation.txt with content "All batches validated".
    Then call exit.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/validate_results.md.
- step: generate_report
  agent: worker
  depends_on:
  - validate_results
  input: |-
    Read the validation file and all batch files from {{var:output_dir}}/long_running/.
    Update {{var:output_dir}}/long_running/config.json to:
    {"total_items": {{var:total_items}}, "status": "completed", "batches_processed": 3}
    Create {{var:output_dir}}/long_running/final_report.txt summarizing all processed batches.
    Then call exit.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/generate_report.md.
error_handling:
  max_retries: 2
  retry_delay: 3s
  continue_on_error: false
timeout:
  total: 900s
  per_step: 180s
directory_isolation:
  enabled: true
  base_dir: ".workflow-runs"
  working_dir_pattern: "{base_dir}/{execution_id}/{step_name}/workspace"
  output_dir_pattern: "{base_dir}/{execution_id}/output"
  persistence_dir_pattern: "{base_dir}/{execution_id}/.snapshots"
  shared_cache_dir: ".prompt_cache"
  shared_history_dir: ".agent_history"
  shared_skills_dir: "./skills"
  cleanup_on_completion: false
  cleanup_after_hours: 72
concurrency:
  enabled: true
  max_concurrency: 4
global_agent:
  enabled: true
  agent:
    preset: universal
    overrides:
      system_prompt: |-
        You are a workflow template assistant that helps users understand, debug, and demo the current workflow.
        When generating or modifying YAML, use braidrun-workflow's snake_case syntax and keep the template runnable.
      tool_set:
      - workflow
      - file_system
      - exit
      max_iterations: 2048
      session_id_strategy: per_execution
