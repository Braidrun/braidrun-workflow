---
name: test-workflow-agent-based
version: 1.0.0
tags:
- test
- agent-based-orchestration
- dynamic-orchestration
- agent_based
description: |
  Agent-based orchestration feature test - tests the Orchestrator Agent dynamically delegating tasks
  Covers: orchestrator (preset + overrides), participants, goal,
  max_steps, budget_tokens, timeout_seconds
translations:
  en:
    name: "Workflow Agent-Based Test"
    description: "Agent-based orchestration feature test - tests the Orchestrator Agent dynamically delegating tasks Covers: orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
  zh:
    name: "工作流基于代理测试"
    description: "基于代理的编排功能测试——测试 Orchestrator Agent 动态委派任务。涵盖：orchestrator（preset + overrides）、participants、goal、max_steps、budget_tokens、timeout_seconds"
  zhHant:
    name: "工作流程基於代理測試"
    description: "基於代理的編排功能測試——測試 Orchestrator Agent 動態委派任務。涵蓋：orchestrator（preset + overrides）、participants、goal、max_steps、budget_tokens、timeout_seconds"
  ja:
    name: "ワークフローエージェントベーステスト"
    description: "エージェントベースのオーケストレーション機能テスト—Orchestrator Agent がタスクを動的に委任する動作をテストします。対象: orchestrator (preset + overrides)、participants、goal、max_steps、budget_tokens、timeout_seconds"
  ko:
    name: "워크플로 에이전트 기반 테스트"
    description: "에이전트 기반 오케스트레이션 기능 테스트 — Orchestrator Agent가 작업을 동적으로 위임하는 동작을 테스트합니다. 범위: orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
  es:
    name: "Prueba de flujo de trabajo basada en agentes"
    description: "Prueba de la función de orquestación basada en agentes: verifica que Orchestrator Agent delega tareas dinámicamente. Cubre: orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
  fr:
    name: "Test de workflow basé sur un agent"
    description: "Test de fonctionnalité d’orchestration basée sur des agents — vérifie qu’Orchestrator Agent délègue les tâches de manière dynamique. Couvre : orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
  de:
    name: "Agentenbasierter Workflow-Test"
    description: "Test der agentenbasierten Orchestrierungsfunktion — prüft, dass Orchestrator Agent Aufgaben dynamisch delegiert. Abgedeckt: orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
  ar:
    name: "اختبار سير العمل القائم على الوكيل"
    description: "اختبار ميزة التنسيق القائم على الوكيل — يختبر قيام Orchestrator Agent بتفويض المهام ديناميكيًا. يشمل: orchestrator (preset + overrides)، participants، goal، max_steps، budget_tokens، timeout_seconds"
  pt:
    name: "Teste de fluxo de trabalho baseado em agente"
    description: "Teste de recurso de orquestração baseada em agente — verifica se Orchestrator Agent delega tarefas dinamicamente. Cobre: orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
  vi:
    name: "Kiểm thử workflow dựa trên tác nhân"
    description: "Kiểm thử tính năng điều phối dựa trên tác nhân — kiểm tra Orchestrator Agent phân công nhiệm vụ một cách động. Bao gồm: orchestrator (preset + overrides), participants, goal, max_steps, budget_tokens, timeout_seconds"
variables:
  project_name: User Authentication Module
  output_dir: "./output"
  demo_mode: 'true'
agents:
  analyst:
    preset: universal
    description: "Handles analysis responsibilities in this test workflow."
    translations:
      en:
        description: "Handles analysis responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的分析职责。"
      zhHant:
        description: "負責此測試工作流程中的分析職責。"
      ja:
        description: "このテストワークフローで分析を担当します。"
      ko:
        description: "이 테스트 워크플로에서 분석 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de análisis en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de analyse dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Analyse."
      ar:
        description: "يتولى مسؤوليات التحليل في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de análise neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò phân tích trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a business analyst. Analyze requirements and identify key features. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  developer:
    preset: universal
    description: "Handles software development responsibilities in this test workflow."
    translations:
      en:
        description: "Handles software development responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的软件开发职责。"
      zhHant:
        description: "負責此測試工作流程中的軟體開發職責。"
      ja:
        description: "このテストワークフローでソフトウェア開発を担当します。"
      ko:
        description: "이 테스트 워크플로에서 소프트웨어 개발 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de desarrollo de software en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de développement logiciel dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Softwareentwicklung."
      ar:
        description: "يتولى مسؤوليات تطوير البرمجيات في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de desenvolvimento de software neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò phát triển phần mềm trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a software developer. Write code and technical documentation. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  tester:
    preset: universal
    description: "Handles quality assurance testing responsibilities in this test workflow."
    translations:
      en:
        description: "Handles quality assurance testing responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的质量保证测试职责。"
      zhHant:
        description: "負責此測試工作流程中的品質保證測試職責。"
      ja:
        description: "このテストワークフローで品質保証テストを担当します。"
      ko:
        description: "이 테스트 워크플로에서 품질 보증 테스트 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de pruebas de aseguramiento de calidad en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de tests d’assurance qualité dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Qualitätssicherungstests."
      ar:
        description: "يتولى مسؤوليات اختبارات ضمان الجودة في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de testes de garantia de qualidade neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò kiểm thử đảm bảo chất lượng trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a QA tester. Create test plans and identify edge cases. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
workflow:
- step: gather_requirements
  agent: analyst
  input: |
    Gather and organize the requirements for "{{var:project_name}}" and list the core features.
    Save the requirements document to {{var:output_dir}}/requirements.txt.
- step: dynamic_implementation
  agent_based:
    orchestrator:
      preset: universal
      overrides:
        system_prompt: |
          You are a project orchestrator. Based on the requirements analysis, dynamically assign development and testing tasks to team members.
          You need to: 1) assign development tasks 2) assign testing tasks 3) assess overall progress
          Use the delegateTask and delegateParallel tools to assign tasks.
          When finished, use the complete tool to report the final result.
        tool_set:
        - exit
        - skill_tools
        - file_system
        max_iterations: 8196
        session_id_strategy: per_execution
    participants:
    - developer
    - tester
    goal: |-
      Based on the requirements analysis: {{steps.gather_requirements.output}}
      Complete the assignment and execution of development and testing tasks for "{{var:project_name}}", and produce a directly presentable demo conclusion.
    max_steps: 10
    budget_tokens: 100000
    timeout_seconds: 300
  depends_on:
  - gather_requirements
- step: quick_review
  agent_based:
    orchestrator:
      preset: universal
      overrides:
        system_prompt: You are the code-review orchestrator. Dynamically assign review tasks. Use the complete tool when finished.
        session_id_strategy: per_execution
    participants:
    - analyst
    - developer
    goal: Quickly review the implementation quality of {{steps.dynamic_implementation.output}}, and produce a directly presentable demo conclusion.
    max_steps: 5
  depends_on:
  - dynamic_implementation
- step: project_summary
  agent: analyst
  input: |
    Based on the following results, generate a project summary report and save it to {{var:output_dir}}/project_summary.txt:
    Requirements: {{steps.gather_requirements.output}}
    Implementation: {{steps.dynamic_implementation.output}}
    Review: {{steps.quick_review.output}}
  depends_on:
  - quick_review
error_handling:
  max_retries: 2
  retry_delay: 3s
  continue_on_error: false
timeout:
  total: 900s
  per_step: 300s
directory_isolation:
  enabled: true
  base_dir: ".workflow-runs"
  working_dir_pattern: "{base_dir}/{execution_id}/{step_name}/workspace"
  output_dir_pattern: "{base_dir}/{execution_id}/output"
  persistence_dir_pattern: "{base_dir}/{execution_id}/.snapshots"
  shared_cache_dir: ".prompt_cache"
  shared_history_dir: ".agent_history"
  shared_skills_dir: "./skills"
  cleanup_on_completion: false
  cleanup_after_hours: 72
concurrency:
  enabled: true
  max_concurrency: 4
global_agent:
  enabled: true
  agent:
    preset: universal
    overrides:
      system_prompt: |-
        You are a workflow template assistant that helps users understand, debug, and demo the current workflow.
        When generating or modifying YAML, use braidrun-workflow's snake_case syntax and keep the template runnable.
      tool_set:
      - workflow
      - file_system
      - exit
      max_iterations: 2048
      session_id_strategy: per_execution
