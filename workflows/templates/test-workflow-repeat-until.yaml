---
name: test-workflow-repeat-until
version: 1.0.0
tags:
- test
- iterative-refinement
- iterative-refinement
- repeat_until
- condition
description: |
  repeat_until iterative-refinement feature test - tests conditional loop execution
  Covers: condition, max_iterations, evaluate_agent, evaluate_prompt,
  extract_pattern, extract_variable
translations:
  en:
    name: "Repeat Until Iteration Test"
    description: "repeat_until iterative-refinement feature test - tests conditional loop execution Covers: condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
  zh:
    name: "重复直到迭代测试"
    description: "repeat_until 迭代优化功能测试 - 测试条件循环执行。涵盖：condition、max_iterations、evaluate_agent、evaluate_prompt、extract_pattern、extract_variable"
  zhHant:
    name: "重複直到迭代測試"
    description: "repeat_until 迭代優化功能測試 - 測試條件迴圈執行。涵蓋：condition、max_iterations、evaluate_agent、evaluate_prompt、extract_pattern、extract_variable"
  ja:
    name: "Repeat Until 反復テスト"
    description: "repeat_until の反復改善機能テスト - 条件付きループ実行をテストします。対象: condition、max_iterations、evaluate_agent、evaluate_prompt、extract_pattern、extract_variable"
  ko:
    name: "Repeat Until 반복 테스트"
    description: "repeat_until 반복 개선 기능 테스트 - 조건부 루프 실행을 테스트합니다. 포함: condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
  es:
    name: "Prueba de iteración hasta repetir"
    description: "Prueba de la función de refinamiento iterativo repeat_until: prueba la ejecución de bucles condicionales. Cubre: condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
  fr:
    name: "Test d’itération Répéter jusqu’à"
    description: "Test de la fonctionnalité d’affinage itératif repeat_until : teste l’exécution conditionnelle en boucle. Couvre : condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
  de:
    name: "Wiederholen-bis-Iterationen-Test"
    description: "repeat_until-Test für iterative Verfeinerung - testet die bedingte Schleifenausführung. Umfasst: condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
  ar:
    name: "اختبار التكرار حتى"
    description: "اختبار ميزة التحسين التكراري repeat_until - يختبر تنفيذ الحلقات الشرطية. يشمل: condition، max_iterations، evaluate_agent، evaluate_prompt، extract_pattern، extract_variable"
  pt:
    name: "Teste de iteração Repetir até"
    description: "Teste do recurso de refinamento iterativo repeat_until - testa a execução de loop condicional. Abrange: condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
  vi:
    name: "Kiểm thử lặp cho đến khi"
    description: "Kiểm thử tính năng tinh chỉnh lặp repeat_until - kiểm thử thực thi vòng lặp có điều kiện. Bao gồm: condition, max_iterations, evaluate_agent, evaluate_prompt, extract_pattern, extract_variable"
variables:
  topic: Applications of AI in education
  quality_threshold: '7'
  output_dir: "./output"
  quality_score: '0'
  summary_done: 'no'
  demo_mode: 'true'
agents:
  writer:
    preset: universal
    description: "Handles content and report writing responsibilities in this test workflow."
    translations:
      en:
        description: "Handles content and report writing responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的内容和报告写作职责。"
      zhHant:
        description: "負責此測試工作流程中的內容與報告撰寫職責。"
      ja:
        description: "このテストワークフローでコンテンツとレポートの作成を担当します。"
      ko:
        description: "이 테스트 워크플로에서 콘텐츠 및 보고서 작성 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de redacción de contenidos e informes en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de rédaction de contenus et de rapports dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Erstellung von Inhalten und Berichten."
      ar:
        description: "يتولى مسؤوليات كتابة المحتوى والتقارير في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de redação de conteúdo e relatórios neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò viết nội dung và báo cáo trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a content writer. Write and improve content based on feedback. Always output quality_score=N where N is your self-assessment (1-10). Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  reviewer:
    preset: universal
    description: "Handles quality review responsibilities in this test workflow."
    translations:
      en:
        description: "Handles quality review responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的质量审查职责。"
      zhHant:
        description: "負責此測試工作流程中的品質審查職責。"
      ja:
        description: "このテストワークフローで品質レビューを担当します。"
      ko:
        description: "이 테스트 워크플로에서 품질 검토 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de revisión de calidad en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de revue qualité dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Qualitätsprüfung."
      ar:
        description: "يتولى مسؤوليات مراجعة الجودة في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de revisão de qualidade neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò đánh giá chất lượng trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a quality reviewer. Evaluate content quality and output a score. Always output quality_score=N where N is your assessment (1-10). Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  editor:
    preset: universal
    description: "Handles content editing responsibilities in this test workflow."
    translations:
      en:
        description: "Handles content editing responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的内容编辑职责。"
      zhHant:
        description: "負責此測試工作流程中的內容編輯職責。"
      ja:
        description: "このテストワークフローでコンテンツ編集を担当します。"
      ko:
        description: "이 테스트 워크플로에서 콘텐츠 편집 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de edición de contenidos en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de révision de contenu dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Inhaltsbearbeitung."
      ar:
        description: "يتولى مسؤوليات تحرير المحتوى في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de edição de conteúdo neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò biên tập nội dung trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are an editor. Polish and finalize content. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
workflow:
- step: draft_article
  agent: writer
  input: |-
    Write an article about "{{var:topic}}".
    The previous score was {{var:quality_score}}; please improve on it.
    Output format: after the article content, append quality_score=N (N is a self-assessed score from 1-10).

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/draft_article.md.
  repeat_until:
    condition: quality_score >= 7
    max_iterations: 3
    evaluate_agent: reviewer
    evaluate_prompt: |
      Assess the quality of the following article, scoring it on depth of content, logic, and readability.
      Output format: quality_score=N (N is 1-10).
      Article content: {{steps.draft_article.output}}
    extract_pattern: quality_score=(\d+)
    extract_variable: quality_score
- step: refine_summary
  agent: writer
  input: |-
    Based on the following article, write a concise summary (no more than 200 words).
    Output summary_done=yes at the end of the summary when you consider it concise enough.
    Article: {{steps.draft_article.output}}

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/refine_summary.md.
  repeat_until:
    condition: summary_done == 'yes'
    max_iterations: 2
    extract_pattern: summary_done=(\w+)
    extract_variable: summary_done
  depends_on:
  - draft_article
- step: final_edit
  agent: editor
  input: |
    Perform final editing and polishing of the following article and summary, and save it to {{var:output_dir}}/final_article.txt:
    Article quality score: {{var:quality_score}}
    Article: {{steps.draft_article.output}}
    Summary: {{steps.refine_summary.output}}
  depends_on:
  - refine_summary
error_handling:
  max_retries: 2
  retry_delay: 3s
  continue_on_error: false
timeout:
  total: 900s
  per_step: 180s
directory_isolation:
  enabled: true
  base_dir: ".workflow-runs"
  working_dir_pattern: "{base_dir}/{execution_id}/{step_name}/workspace"
  output_dir_pattern: "{base_dir}/{execution_id}/output"
  persistence_dir_pattern: "{base_dir}/{execution_id}/.snapshots"
  shared_cache_dir: ".prompt_cache"
  shared_history_dir: ".agent_history"
  shared_skills_dir: "./skills"
  cleanup_on_completion: false
  cleanup_after_hours: 72
concurrency:
  enabled: true
  max_concurrency: 4
global_agent:
  enabled: true
  agent:
    preset: universal
    overrides:
      system_prompt: |-
        You are a workflow template assistant that helps users understand, debug, and demo the current workflow.
        When generating or modifying YAML, use braidrun-workflow's snake_case syntax and keep the template runnable.
      tool_set:
      - workflow
      - file_system
      - exit
      max_iterations: 2048
      session_id_strategy: per_execution
