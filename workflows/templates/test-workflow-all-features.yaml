---
name: test-workflow-all-features
version: 1.0.0
tags:
- test
- comprehensive-features
- multi-role-collaboration
- iterative-refinement
- deterministic-compute
- smart-routing
- batch-processing
- multi-source-aggregation
- knowledge-retrieval
- parallel-processing
- group_chat
- repeat_until
- extract
- code
- classifier
- iterate_over
- aggregate
- knowledge_base
- parallel
- condition
description: |
  Comprehensive new-feature test - combines all new features in a single workflow
  Covers: group_chat, repeat_until, agent_based, code, extract,
  classifier, iterate_over, aggregate, knowledge_base
translations:
  en:
    name: "Workflow All Features Test"
    description: "Comprehensive new-feature test - combines all new features in a single workflow Covers: group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
  zh:
    name: "工作流全部功能测试"
    description: "综合新功能测试——在一个工作流中组合所有新功能。涵盖：group_chat、repeat_until、agent_based、code、extract、classifier、iterate_over、aggregate、knowledge_base"
  zhHant:
    name: "工作流程全部功能測試"
    description: "綜合新功能測試——在一個工作流程中組合所有新功能。涵蓋：group_chat、repeat_until、agent_based、code、extract、classifier、iterate_over、aggregate、knowledge_base"
  ja:
    name: "ワークフロー全機能テスト"
    description: "包括的な新機能テスト—1 つのワークフローで新機能をすべて組み合わせます。対象: group_chat、repeat_until、agent_based、code、extract、classifier、iterate_over、aggregate、knowledge_base"
  ko:
    name: "워크플로 전체 기능 테스트"
    description: "종합 신규 기능 테스트 — 하나의 워크플로에서 모든 새 기능을 결합합니다. 범위: group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
  es:
    name: "Prueba de todas las funciones del flujo de trabajo"
    description: "Prueba integral de nuevas funciones: combina todas las funciones nuevas en un solo flujo de trabajo. Cubre: group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
  fr:
    name: "Test de toutes les fonctionnalités du workflow"
    description: "Test complet des nouvelles fonctionnalités — combine toutes les nouvelles fonctionnalités dans un seul workflow. Couvre : group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
  de:
    name: "Workflow-Test aller Funktionen"
    description: "Umfassender Test neuer Funktionen — kombiniert alle neuen Funktionen in einem einzigen Workflow. Abgedeckt: group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
  ar:
    name: "اختبار جميع ميزات سير العمل"
    description: "اختبار شامل للميزات الجديدة — يجمع جميع الميزات الجديدة في سير عمل واحد. يشمل: group_chat، repeat_until، agent_based، code، extract، classifier، iterate_over، aggregate، knowledge_base"
  pt:
    name: "Teste de todas as funcionalidades do fluxo de trabalho"
    description: "Teste abrangente de novos recursos — combina todos os novos recursos em um único fluxo de trabalho. Cobre: group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
  vi:
    name: "Kiểm thử tất cả tính năng workflow"
    description: "Kiểm thử tính năng mới toàn diện — kết hợp tất cả tính năng mới trong một workflow duy nhất. Bao gồm: group_chat, repeat_until, agent_based, code, extract, classifier, iterate_over, aggregate, knowledge_base"
variables:
  project_name: AllFeaturesTest
  requirement_text: We need a multi-language user authentication system supporting OAuth2 and traditional password login
  output_dir: "./output"
  quality_score: '0'
  quality_threshold: '7'
  project_type: ''
  demo_mode: 'true'
agents:
  pm:
    preset: universal
    description: "Handles product management responsibilities in this test workflow."
    translations:
      en:
        description: "Handles product management responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的产品管理职责。"
      zhHant:
        description: "負責此測試工作流程中的產品管理職責。"
      ja:
        description: "このテストワークフローでプロダクト管理を担当します。"
      ko:
        description: "이 테스트 워크플로에서 제품 관리 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de gestión de productos en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de gestion de produit dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Produktmanagement."
      ar:
        description: "يتولى مسؤوليات إدارة المنتج في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de gestão de produtos neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò quản lý sản phẩm trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a product manager. Define requirements and manage product direction. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  developer:
    preset: universal
    description: "Handles software development responsibilities in this test workflow."
    translations:
      en:
        description: "Handles software development responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的软件开发职责。"
      zhHant:
        description: "負責此測試工作流程中的軟體開發職責。"
      ja:
        description: "このテストワークフローでソフトウェア開発を担当します。"
      ko:
        description: "이 테스트 워크플로에서 소프트웨어 개발 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de desarrollo de software en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de développement logiciel dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Softwareentwicklung."
      ar:
        description: "يتولى مسؤوليات تطوير البرمجيات في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de desenvolvimento de software neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò phát triển phần mềm trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a software developer. Write code and technical specs. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  tester:
    preset: universal
    description: "Handles quality assurance testing responsibilities in this test workflow."
    translations:
      en:
        description: "Handles quality assurance testing responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的质量保证测试职责。"
      zhHant:
        description: "負責此測試工作流程中的品質保證測試職責。"
      ja:
        description: "このテストワークフローで品質保証テストを担当します。"
      ko:
        description: "이 테스트 워크플로에서 품질 보증 테스트 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de pruebas de aseguramiento de calidad en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de tests d’assurance qualité dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Qualitätssicherungstests."
      ar:
        description: "يتولى مسؤوليات اختبارات ضمان الجودة في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de testes de garantia de qualidade neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò kiểm thử đảm bảo chất lượng trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a QA tester. Create test plans and verify quality. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
  classifier_agent:
    preset: universal
    description: "Handles input classification responsibilities in this test workflow."
    translations:
      en:
        description: "Handles input classification responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的输入分类职责。"
      zhHant:
        description: "負責此測試工作流程中的輸入分類職責。"
      ja:
        description: "このテストワークフローで入力分類を担当します。"
      ko:
        description: "이 테스트 워크플로에서 입력 분류 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de clasificación de entradas en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de classification des entrées dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Eingabeklassifizierung."
      ar:
        description: "يتولى مسؤوليات تصنيف المدخلات في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de classificação de entradas neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò phân loại đầu vào trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a classification expert. Classify inputs accurately. Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      max_iterations: 8196
      session_id_strategy: auto
  reviewer:
    preset: universal
    description: "Handles quality review responsibilities in this test workflow."
    translations:
      en:
        description: "Handles quality review responsibilities in this test workflow."
      zh:
        description: "负责此测试工作流中的质量审查职责。"
      zhHant:
        description: "負責此測試工作流程中的品質審查職責。"
      ja:
        description: "このテストワークフローで品質レビューを担当します。"
      ko:
        description: "이 테스트 워크플로에서 품질 검토 역할을 담당합니다."
      es:
        description: "Se encarga de las responsabilidades de revisión de calidad en este flujo de trabajo de prueba."
      fr:
        description: "Prend en charge les responsabilités de revue qualité dans ce workflow de test."
      de:
        description: "Übernimmt in diesem Test-Workflow die Verantwortung für Qualitätsprüfung."
      ar:
        description: "يتولى مسؤوليات مراجعة الجودة في سير عمل الاختبار هذا."
      pt:
        description: "Cuida das responsabilidades de revisão de qualidade neste workflow de teste."
      vi:
        description: "Đảm nhiệm vai trò đánh giá chất lượng trong workflow kiểm thử này."
    overrides:
      system_prompt: |-
        You are a quality reviewer. Evaluate quality and output quality_score=N (1-10). Call exit when done.

        Common requirements:
        - The template's first goal is to run and demo directly.
        - If real-time data, external accounts, API credentials, or target-system access are missing, continue using the current input, workflow context, and reasonable assumptions; do not stop to ask for external dependencies.
        - Prefer self-contained, presentable, reusable demo output, and write it to workspace files when appropriate.
      tool_set:
      - exit
      - skill_tools
      - file_system
      max_iterations: 8196
      session_id_strategy: auto
workflow:
- step: classify_project
  classifier:
    agent: classifier_agent
    input: |-
      Classify the following requirement by project type:
      {{var:requirement_text}}

      Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/classify_project.md.
    categories:
    - name: backend
      description: Backend service project, covering APIs, databases, and server-side logic
    - name: frontend
      description: Frontend project, covering UI, user interaction, and page design
    - name: fullstack
      description: Full-stack project, covering both frontend and backend
    output_variable: project_type
    default_category: fullstack
- step: setup_project
  code:
    language: bash
    script: |
      mkdir -p ${WF_VAR_OUTPUT_DIR}/${WF_VAR_PROJECT_NAME}
      echo "project=${WF_VAR_PROJECT_NAME}" > ${WF_VAR_OUTPUT_DIR}/${WF_VAR_PROJECT_NAME}/config.txt
      echo "type=${WF_VAR_PROJECT_TYPE}" >> ${WF_VAR_OUTPUT_DIR}/${WF_VAR_PROJECT_NAME}/config.txt
      echo "status=initialized"
      echo "setup_complete=true"
    timeout: 10
  extract:
  - pattern: setup_complete=(\w+)
    variable: setup_status
  depends_on:
  - classify_project
- step: requirement_review
  group_chat:
    participants:
    - pm
    - developer
    - tester
    moderator: pm
    max_rounds: 3
    speaker_selection: round_robin
    termination_keyword: CONSENSUS_REACHED
    initial_message: |-
      Project type: {{var:project_type}}
      Requirement description: {{var:requirement_text}}
      Everyone, please review this requirement and discuss technical feasibility and the testing strategy.
      Output CONSENSUS_REACHED once consensus is reached.

      At the end of the discussion, give a reusable summary of the conclusion so later steps can save it directly as a demo deliverable.
  depends_on:
  - setup_project
- step: design_iteration
  agent: developer
  input: |-
    Based on the requirements review, design the technical solution for "{{var:project_name}}".
    Project type: {{var:project_type}}
    Review comments: {{steps.requirement_review.output}}
    Previous quality score: {{var:quality_score}}
    Improve the design and output quality_score=N (N is 1-10).

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/design_iteration.md.
  repeat_until:
    condition: quality_score >= 7
    max_iterations: 3
    evaluate_agent: reviewer
    evaluate_prompt: |
      Assess the quality of the following technical design:
      {{steps.design_iteration.output}}
      Output quality_score=N (N is 1-10).
    extract_pattern: quality_score=(\d+)
    extract_variable: quality_score
  depends_on:
  - requirement_review
- step: generate_tasks
  code:
    language: python
    script: |
      import os
      project_type = os.environ.get('WF_VAR_PROJECT_TYPE', 'fullstack')
      tasks = []
      if project_type in ('backend', 'fullstack'):
          tasks.extend(['Design the database schema', 'Implement the auth API', 'Write backend unit tests'])
      if project_type in ('frontend', 'fullstack'):
          tasks.extend(['Design the login page', 'Implement the OAuth2 frontend flow', 'Write frontend E2E tests'])
      print('\n'.join(tasks))
    timeout: 10
  depends_on:
  - design_iteration
- step: execute_tasks
  agent: developer
  input: |-
    Execute development task #{{var:dev_task_index}}: {{var:dev_task_item}}
    Project: {{var:project_name}}
    Output a task-completion report.

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/execute_tasks.md.
  iterate_over:
    source: "{{steps.generate_tasks.output}}"
    delimiter: "\n"
    item_variable: dev_task_item
    index_variable: dev_task_index
    max_items: 10
    parallel: false
    results_variable: task_results
  depends_on:
  - generate_tasks
- step: verify_security
  agent: tester
  input: |-
    Perform security verification of "{{var:project_name}}".
    Design proposal: {{steps.design_iteration.output}}
    Development results: {{var:task_results}}

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/verify_security.md.
  depends_on:
  - execute_tasks
- step: verify_functionality
  agent: tester
  input: |-
    Perform functional verification of "{{var:project_name}}".
    Design proposal: {{steps.design_iteration.output}}
    Development results: {{var:task_results}}

    Demo constraint: if external dependencies are missing, produce a presentable demo result directly; when appropriate, also save it to {{var:output_dir}}/verify_functionality.md.
  depends_on:
  - execute_tasks
- step: aggregate_results
  agent: pm
  input: |
    Summarize the following verification results, generate the final project report, and save it to {{var:output_dir}}/{{var:project_name}}/final_report.txt:
    Project type: {{var:project_type}}
    Quality score: {{var:quality_score}}
    Aggregated validation result: {{var:aggregated_verification}}
  aggregate:
    sources:
    - "{{steps.verify_security.output}}"
    - "{{steps.verify_functionality.output}}"
    strategy: numbered_list
    output_variable: aggregated_verification
  depends_on:
  - verify_security
  - verify_functionality
error_handling:
  max_retries: 2
  retry_delay: 3s
  continue_on_error: false
timeout:
  total: 1200s
  per_step: 180s
directory_isolation:
  enabled: true
  base_dir: ".workflow-runs"
  working_dir_pattern: "{base_dir}/{execution_id}/{step_name}/workspace"
  output_dir_pattern: "{base_dir}/{execution_id}/output"
  persistence_dir_pattern: "{base_dir}/{execution_id}/.snapshots"
  shared_cache_dir: ".prompt_cache"
  shared_history_dir: ".agent_history"
  shared_skills_dir: "./skills"
  cleanup_on_completion: false
  cleanup_after_hours: 72
concurrency:
  enabled: true
  max_concurrency: 4
knowledge_base:
  enabled: true
  storage_dir: "./kb-all-features"
  auto_index_outputs: true
  auto_inject_rag_tools: true
  chunk_size: 500
  source_files:
  - path: "./WORKFLOW_GUIDE.md"
    tags: workflow,syntax
global_agent:
  enabled: true
  agent:
    preset: universal
    overrides:
      system_prompt: |-
        You are a workflow template assistant that helps users understand, debug, and demo the current workflow.
        When generating or modifying YAML, use braidrun-workflow's snake_case syntax and keep the template runnable.
      tool_set:
      - workflow
      - file_system
      - exit
      max_iterations: 2048
      session_id_strategy: per_execution
